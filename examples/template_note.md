### Reviewer template file note

You provided: `reviewTemplate_OriginalResearch.doc` at `/mnt/data/reviewTemplate_OriginalResearch.doc`.

This file is `.doc` (legacy Word format). `python-docx` reads `.docx` only.
Options:
1) Save/Export it as `.docx`, then place it under `config/prompts/` and extract it to text for prompt inclusion.
2) Or paste the template text into `config/prompts/reviewer_template.txt`.

The pipeline already follows your template logic in the prompts; embedding the exact template text is optional.
